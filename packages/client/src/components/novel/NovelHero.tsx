// 短剧 Agent 页顶部标题区
// 渲染短剧 Agent 标题
export function NovelHero() {
    return (
        <header className="flex w-full flex-col items-center gap-4 text-center">
            <h1 className="text-[clamp(2.25rem,4.5vw,3rem)] leading-tight font-medium tracking-wide text-slate-900">
                短剧Agent
            </h1>
        </header>
    );
}
